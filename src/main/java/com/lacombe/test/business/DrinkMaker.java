package com.lacombe.test.business;

public class DrinkMaker {

	/**
	 * fore now we assumes that the machine is perfect, therefore, it will always
	 * returns true
	 * 
	 * @param command: the drink to be prepared with all specified options
	 * @return wither the drink was made, or there is a probleme,
	 */
	public boolean makeDrink(String command) {

		return true;
	}

}
